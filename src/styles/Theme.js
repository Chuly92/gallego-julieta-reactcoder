import { createTheme } from '@mui/material';
import React from 'react';

export const theme = createTheme({
  palette: {
    primary: {
      light: "#e0e0e0",
      main: "#697083",
      dark: "#171e2d",
    },
    secondary: {
      light: "#f59ab4",
      main: "#bc5474",
      dark: "#892449",
    },
  },
});

export const Theme = () => {
  return (<></>)
};
